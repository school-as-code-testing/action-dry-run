console.log("setting up repository:", process.argv[2], process.argv[3]);

// create labels

// create milestones
//  update config to store each milestone's id in materials.path entries

// create boards
//  create a boards config if there was none
//  create a main board if there was none
//  create the boards
//  update boards config to include id's and/or new board

// create teams

// persist & commit the new config
